swagger: '2.0'
info:
  description: ''
  version: '2'
  title: COVID-19
host: 'localhost:8080'
tags:
  - name: Patients 
schemes:
  - http
paths:
  /ShowPatientsData:
    get:
      tags:
        - Patients
      summary: สรุปจากฐานข้อมูลของผู้ป่วย
      consumes:
        - application/x-www-form-urlencoded
      produces:
        - application/json
      responses:
        '200':
          description: Update
          schema:
            $ref: '#/definitions/transaction200'
        '400':
          description: Bad Request
          schema:
            $ref: '#/definitions/badRequest400'
        '500':
          description: Internal Server Error
          schema:
            $ref: '#/definitions/transaction500'
definitions:
  badRequest400:
    type: object
    properties:
      statusCode:
        type: number
        example: 400
      message:
        type: string
        example: <function_name> getPatientData is not a function
  transaction200:
    type: object
    properties:
      statusCode:
        type: number
        example: 200
      message:
        type: string
        example: 'ข้อมูลที่ดึงออกมา'
  transaction500:
    type: object
    properties:
      statusCode:
        type: number
        example: 500
      message:
        type: string
        example: 'Internal Server Error'
      
